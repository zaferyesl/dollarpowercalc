# Optional: CPI series via St. Louis Fed (enables ISR-cached CPIAUCSL pulls once per deployment day)
FRED_API_KEY=

# Optional: override default GA4 ID (defaults to production ID in lib/site.ts if unset)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
